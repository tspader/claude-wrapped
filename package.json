{
  "name": "@spader/claude-wrapped",
  "version": "1.0.0",
  "module": "index.ts",
  "type": "module",
  "bin": {
    "claude-wrapped": "./src/main.ts"
  },
  "files": [
    "src/**/*"
  ],
  "scripts": {
    "build:wasm": "clang --target=wasm32 -O3 -msimd128 -nostdlib -Wl,--no-entry -Wl,--export-all -o src/wasm/renderer.wasm src/wasm/renderer.c",
    "start": "bun run build:wasm && bun src/main.ts -a"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251213.0",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@opentui/core": "^0.1.60",
    "simplex-noise": "^4.0.3"
  }
}
